# Backend API base URL (used for Socket.IO and API calls)
VITE_BASE_URL=http://localhost:3000
# Google OAuth client ID for login (optional; leave empty if not using Google login)
VITE_LOGIN_OAUTH_ID=
